import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('KoVoit smoke test', (WidgetTester tester) async {
    // Test de base — le widget réel nécessite ProviderScope et GoRouter
    expect(true, isTrue);
  });
}
